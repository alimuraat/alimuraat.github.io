Astro writes hashed assets into _astro/, and GitHub Pages runs Jekyll by
default, which silently drops every path beginning with an underscore. This
file turns Jekyll off. Without it the site deploys with no CSS and no JS.
