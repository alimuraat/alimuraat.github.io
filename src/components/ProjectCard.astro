---
interface Props {
  title: string;
  year: string;
  body: string;
}

const { title, year, body } = Astro.props;
---

<article class="card">
  <h3>{title}</h3>
  <span class="year">{year}</span>
  <p>{body}</p>
</article>
